package MyUtil.report;

import android.text.TextUtils;
import android.util.Log;


public class CreateHeaders {
    private String aa;

    public CreateHeaders() {
        String upperCase = a.aa().toUpperCase();
        this.aa = upperCase;
        Log.e("[CreateHeaders]", "this.a->" + this.aa);
    }


    private void e(String str) {
        c.b(str);
    }

    public String aa() {
        String str;
        str = TextUtils.isEmpty(this.aa) ? "" : this.aa; // 读取当前对象中(内存)有没有buvid
        e(str);
        return str;
    }
}
